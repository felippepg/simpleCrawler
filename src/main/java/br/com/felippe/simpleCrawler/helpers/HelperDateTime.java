package br.com.felippe.simpleCrawler.helpers;

import org.jsoup.nodes.Element;

import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;

public class HelperDateTime {
//    Element timeElement = visited.selectFirst("time.entry-date");
//    String datetimeAttribute = timeElement.attr("datetime");
//    LocalDateTime dateTime = LocalDateTime.parse(datetimeAttribute, DateTimeFormatter.ISO_OFFSET_DATE_TIME);
//
//    DateTimeFormatter formatter = DateTimeFormatter.ofPattern("dd/MM/yyyy HH:mm");
//    String formattedDateTime = dateTime.format(formatter);
//        System.out.println(formattedDateTime);
}
